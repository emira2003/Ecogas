// Next.js App Router ships React canary, which includes <ViewTransition>.
// The stable @types/react keeps those typings in a separate file, and this pulls them in.
/// <reference types="react/canary" />
